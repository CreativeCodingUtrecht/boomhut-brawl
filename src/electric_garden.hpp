#pragma once

#include "sprite_autounload.hpp"
#include "bn_sprite_animate_actions.h"

#include "bn_sprite_items_e_garden_power.h"


struct electric_garden {
    // bn::optional<bn::spr_poin e_garden = sprite_autounload(bn::sprite_items::e_garden_power, bn::fixed_point(400,490));
    
    // void update() {
    //     e_garden.update_visibility();
    //     if (e_garden_anim)
            // if (abs(camera->x() - position.x()) < 140 && abs(camera->y() - position.y()) < 100) {
    // }

    
};